{
  "name": "slack-ghost",
  "version": "0.0.0",
  "description": "POC for Facebook Messaenger quiz bot",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "DEBUG=${DEBUG:-slackghost:*,messenger*} nodemon -x npm start",
    "lint": "eslint --ignore-path .gitignore .",
    "pretest": "npm run lint",
    "test": "NODE_ENV=test env $(cat example.env | xargs) istanbul cover _mocha -- --recursive",
    "tdd": "NODE_ENV=test DEBUG=${DEBUG:-quizshow:*error} env $(cat example.env | xargs) mocha --recursive --watch --reporter spec --bail"
  },
  "engines": {
    "node": ">6.0"
  },
  "author": "Condé Nast Partnerships Team",
  "license": "Apache-2.0",
  "dependencies": {
    "@slack/client": "^3.8.1",
    "debug": "^2.6.1",
    "launch-vehicle-fbm": "^1.0.0",
    "request": "^2.81.0",
    "request-promise": "^4.1.1"
  },
  "devDependencies": {
    "flow-bin": "^0.41.0",
    "husky": "^0.13.2",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "sinon": "^1.17.6"
  },
  "private": true
}
